import { styled } from "@mui/system";

export const OverviewGraphWrapper = styled("div")(() => ({
  minHeight: "400px",
  background: "#6a5daf",
  borderRadius: "20px",
  padding: "10px",
  paddingTop: "40px",
}));
